/**
 *  Для данной строки нужно проверить, что она содержит одинаковое число `x` и `o`.
 * Регистр не имеет значения.
 * Если число вхождений одинаковое - вернуть true, иначе false
 * @param {string} str - строка для проверки на палиндром
 * @returns {boolean}
 */
export function isEqualXAndO(str) {}
